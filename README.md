# 🧠 Система розпізнавання рукописних цифр з використанням нейронних мереж

Проект для розпізнавання рукописних цифр (0-9) за допомогою згорткової нейронної мережі (CNN), навченої на датасеті MNIST.

---

## 📦 Встановлення

1. Клонуйте репозиторій або завантажте файли проекту

2. Встановіть залежності:

```bash
pip install -r requirements.txt
```

Або встановіть вручну:

```bash
pip install tensorflow numpy matplotlib Pillow
```

---

## 📄 Опис файлів

### `mnist_cnn.py`

**Призначення:** Навчання згорткової нейронної мережі для розпізнавання рукописних цифр.

**Що робить:**

- Завантажує датасет MNIST (60,000 тренувальних та 10,000 тестових зображень розміром 28×28 пікселів)
- Підготовлює дані: нормалізує пікселі (0-255 → 0-1) та конвертує мітки в категорійний формат
- Створює архітектуру CNN:
  - **Conv2D** (32 фільтри, 3×3) + ReLU активація
  - **MaxPooling2D** (2×2)
  - **Conv2D** (64 фільтри, 3×3) + ReLU активація
  - **MaxPooling2D** (2×2)
  - **Flatten** (перетворення в вектор)
  - **Dense** (128 нейронів) + ReLU активація
  - **Dense** (10 нейронів) + Softmax активація (вихідний шар)
- Компілює модель з оптимізатором Adam та функцією втрат categorical_crossentropy
- Навчає модель протягом 5 епох з batch_size=64 та validation_split=0.1
- Оцінює модель на тестовому наборі
- Зберігає навчену модель у файл `mnist_cnn_model.keras`
- Зберігає історію навчання у файл `training_history.pkl`

**Очікувана точність:** ~98-99% на тестовому наборі

**Запуск:**

```bash
python mnist_cnn.py
```

**Час виконання:** ~5-10 хвилин (залежить від апаратного забезпечення)

**Результат:**

- `mnist_cnn_model.keras` - навчена модель (~4.8 MB)
- `training_history.pkl` - історія навчання для аналізу

---

### `draw_and_predict.py`

**Призначення:** Інтерактивний інструмент для малювання цифр мишкою та їх миттєвого розпізнавання.

**Що робить:**

- Завантажує навчену модель з файлу `mnist_cnn_model.keras`
- Відкриває графічне вікно з двома панелями:
  - **Ліва панель:** Полотно для малювання (280×280 пікселів)
  - **Права панель:** Графік ймовірностей для кожної цифри (0-9)
- Дозволяє малювати цифри мишкою з товщиною лінії 20 пікселів
- Автоматично обробляє намальоване зображення:
  - Знаходить bounding box цифри
  - Вирізає та центрує цифру
  - Масштабує до 28×28 пікселів (як у MNIST)
  - Нормалізує значення пікселів
- Виконує передбачення та відображає:
  - Найбільш ймовірну цифру з відсотком впевненості
  - Графік ймовірностей для всіх 10 цифр
  - Детальну інформацію в консолі
- Має кнопки:
  - **"Очистити"** - очищає полотно та графік
  - **"Розпізнати"** - виконує передбачення

**Запуск:**

```bash
python draw_and_predict.py
```

**Вимоги:** Модель `mnist_cnn_model.keras` повинна існувати (спочатку запустіть `mnist_cnn.py`)

**Інструкція по використанню:**

1. Намалюйте цифру (0-9) мишкою на лівому полі
2. Натисніть кнопку **"Розпізнати"** для отримання передбачення
3. Натисніть кнопку **"Очистити"** для нового малювання

**💡 Поради для кращого розпізнавання:**

- Малюйте великі цифри, використовуючи все доступне полотно
- Використовуйте товсті суцільні лінії без розривів
- Малюйте цифри в друкованому стилі (не курсивом)
- Центруйте цифру на полотні

---

## 🚀 Швидкий старт

### Крок 1: Навчання моделі

```bash
python mnist_cnn.py
```

Це займе кілька хвилин. Після завершення ви побачите:

- Точність моделі на тестовому наборі
- Збережену модель у файлі `mnist_cnn_model.keras`

### Крок 2: Тестування моделі

```bash
python draw_and_predict.py
```

Відкриється вікно, де ви зможете:

- Намалювати цифру мишкою
- Отримати миттєве розпізнавання
- Побачити ймовірності для всіх цифр

---

## 📖 Детальне використання

### Навчання моделі (`mnist_cnn.py`)

**Параметри навчання (можна змінити в коді):**

- `epochs=5` - кількість епох навчання
- `batch_size=64` - розмір батчу
- `validation_split=0.1` - частка даних для валідації (10%)

**Модифікація архітектури:**
Ви можете змінити архітектуру моделі в файлі `mnist_cnn.py`:

- Додати більше згорткових шарів
- Змінити кількість фільтрів
- Додати Dropout для зменшення перенавчання
- Змінити розмір Dense шарів

**Приклад модифікації:**

```python
model = Sequential([
    Conv2D(32, (3,3), activation='relu', input_shape=(28,28,1)),
    MaxPooling2D(pool_size=(2,2)),
    Conv2D(64, (3,3), activation='relu'),
    MaxPooling2D(pool_size=(2,2)),
    Conv2D(128, (3,3), activation='relu'),  # Додатковий шар
    Flatten(),
    Dense(256, activation='relu'),  # Більший шар
    Dense(10, activation='softmax')
])
```

### Інтерактивне малювання (`draw_and_predict.py`)

**Особливості обробки зображення:**

- Автоматичне знаходження меж цифри
- Центрування та масштабування з збереженням пропорцій
- Нормалізація пікселів до діапазону [0, 1]

**Вихідні дані:**

- Передбачена цифра з відсотком впевненості
- Графік ймовірностей для всіх 10 цифр
- Детальний вивід у консолі з усіма ймовірностями

---

## 📁 Структура проекту

```
code/
│
├── mnist_cnn.py              # Навчання CNN моделі
├── draw_and_predict.py       # Інтерактивне малювання та розпізнавання
└── README.md                 # Цей файл
```
